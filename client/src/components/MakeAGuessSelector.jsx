import React from 'react';

class MakeAGuessSelector extends React.Component {
  render() {
    return (
      <select id="make_a_guess">
        <option>Make a guess?</option>
      </select>
    );
  }
}

export default MakeAGuessSelector;